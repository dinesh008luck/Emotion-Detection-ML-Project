from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='First projcet with ML pipe to detection the emotion',
    author='Dinesh Kumar',
    license='',
)
